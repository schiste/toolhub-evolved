{
	"$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
	"_comment": "Mutation testing via the Vitest runner: per-test coverage means only the tests covering a mutant rerun, so mutating the whole app stays fast in CI.",
	"testRunner": "vitest",
	"coverageAnalysis": "perTest",
	"mutate": ["public_html/lib/core/**/*.js"],
	"reporters": ["clear-text", "progress"],
	"_thresholds_note": "break is a ratchet floor; raised as tests are added (Phase 3 targets 100% across all layers).",
	"thresholds": {
		"high": 80,
		"low": 40,
		"break": 20
	}
}
